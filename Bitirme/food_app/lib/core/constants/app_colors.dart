import 'package:flutter/material.dart';

class AppColors {
  static const appBackground = Color(0xFFFFFDFD);
  static const primaryColor = Color(0xFFF37B2D);
  static const primaryTextColor = Colors.black;
  static const secondaryTextColor = Colors.white;
  static const primaryTextButtonColor = buttonColor;
  static const secondaryTextButtonColor = primaryTextColor;
  static const buttonColor = Color(0xFFEEA634);
  static const buttonTextColor = secondaryTextColor;
}
